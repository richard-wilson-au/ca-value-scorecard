'use client';

export default function SoftSignoff() {
  return (
    <section className="bg-tp-white">
      <p className="font-sans text-sm font-light leading-relaxed text-tp-navy-soft md:text-base">
        Richard reads every reply. If anything in this surfaced a question,
        just reply to the email — I&apos;ll see it.
      </p>
    </section>
  );
}
